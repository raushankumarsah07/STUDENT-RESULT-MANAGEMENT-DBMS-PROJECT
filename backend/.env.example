# Copy this file to .env and fill in your values
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=student_result_db
DB_USER=postgres
DB_PASSWORD=0709

# Server Configuration
# Port 5001 avoids conflict with macOS AirPlay Receiver (which uses 5000)
PORT=5001
